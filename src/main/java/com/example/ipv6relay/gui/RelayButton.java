package com.example.ipv6relay.gui;

/**
 * 按键绑定类（已禁用，仅保留空壳避免引用报错）
 */
public class RelayButton {
    public static void init() {
        // 不再注册按键，仅通过暂停菜单按钮打开 GUI
    }

    public static void registerKeyMappings(Object event) {
        // 不再注册按键
    }
}
